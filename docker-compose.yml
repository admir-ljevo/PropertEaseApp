version: '3.9'

services:

  # ─────────────────────── SQL SERVER ───────────────────────
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-CU14-ubuntu-22.04
    container_name: propertease-sqlserver
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: "${SA_PASSWORD:-PropertEase_Dev#2024}"
      MSSQL_PID: "Developer"
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql
    networks:
      - propertease-net
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P \"$$SA_PASSWORD\" -Q 'SELECT 1' -No || /opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P \"$$SA_PASSWORD\" -Q 'SELECT 1' || exit 1"]
      interval: 15s
      retries: 12
      start_period: 60s

  # ─────────────────────── RABBITMQ ─────────────────────────
  rabbitmq:
    image: rabbitmq:3.12.13-management
    container_name: propertease-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: "${RABBITMQ_USER:-guest}"
      RABBITMQ_DEFAULT_PASS: "${RABBITMQ_PASS:-guest}"
    ports:
      - "5672:5672"
      - "15672:15672"   # Management UI
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - propertease-net
    healthcheck:
      test: rabbitmq-diagnostics -q ping
      interval: 10s
      retries: 15
      start_period: 30s

  # ─────────────────────── MAIN API ─────────────────────────
  api:
    build:
      context: .
      dockerfile: PropertEaseApi/Dockerfile
    container_name: propertease-api
    environment:
      ASPNETCORE_ENVIRONMENT: "Production"
      ASPNETCORE_URLS: "http://+:80"
      ConnectionStrings__DefaultConnection: "Server=sqlserver;Database=190049;User Id=sa;Password=${SA_PASSWORD:-PropertEase_Dev#2024};TrustServerCertificate=True;MultipleActiveResultSets=true"
      JWTConfig__TokenKey: "${JWT_TOKEN_KEY:-change-this-in-production-must-be-32-chars}"
      JWTConfig__TokenValidityInMinutes: "120"
      RabbitMQ__Host: "rabbitmq"
      RabbitMQ__Port: "5672"
      RabbitMQ__Username: "${RABBITMQ_USER:-guest}"
      RabbitMQ__Password: "${RABBITMQ_PASS:-guest}"
      RabbitMQ__Exchange: "propertease.exchange"
      Uploads__DirectoryPath: "uploads/images"
      ApiBaseUrl: "http://api:80"
      PayPal__ClientId: "${PayPal__ClientId}"
      PayPal__Secret: "${PayPal__Secret}"
      PayPal__Mode: "${PayPal__Mode:-sandbox}"
      Smtp__Host: "${SMTP_HOST:-smtp.gmail.com}"
      Smtp__Port: "${SMTP_PORT:-587}"
      Smtp__Username: "${SMTP_USERNAME}"
      Smtp__Password: "${SMTP_PASSWORD}"
      Smtp__FromName: "PropertEase"
      Logging__LogLevel__Default: "Information"
      Logging__LogLevel__Microsoft.AspNetCore: "Warning"
      Logging__LogLevel__Microsoft.EntityFrameworkCore.Database.Command: "Warning"
    ports:
      - "5000:80"
    depends_on:
      sqlserver:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    volumes:
      - uploads_data:/app/wwwroot/uploads
    networks:
      - propertease-net
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"

  # ─────────────────────── WORKER SERVICE ───────────────────
  worker:
    build:
      context: .
      dockerfile: PropertEase.Worker/Dockerfile
    container_name: propertease-worker
    environment:
      DOTNET_ENVIRONMENT: "Production"
      ConnectionStrings__DefaultConnection: "Server=sqlserver;Database=190049;User Id=sa;Password=${SA_PASSWORD:-PropertEase_Dev#2024};TrustServerCertificate=True;MultipleActiveResultSets=true"
      RabbitMQ__Host: "rabbitmq"
      RabbitMQ__Port: "5672"
      RabbitMQ__Username: "${RABBITMQ_USER:-guest}"
      RabbitMQ__Password: "${RABBITMQ_PASS:-guest}"
      Smtp__Host: "${SMTP_HOST:-smtp.gmail.com}"
      Smtp__Port: "${SMTP_PORT:-587}"
      Smtp__Username: "${SMTP_USERNAME}"
      Smtp__Password: "${SMTP_PASSWORD}"
      Smtp__FromName: "PropertEase"
      Logging__LogLevel__Default: "Information"
    depends_on:
      rabbitmq:
        condition: service_healthy
      sqlserver:
        condition: service_healthy
    networks:
      - propertease-net
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"

volumes:
  sqlserver_data:
  rabbitmq_data:
  uploads_data:

networks:
  propertease-net:
    driver: bridge
